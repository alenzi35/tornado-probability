name: Process RAP Tornado Data

on:
  workflow_dispatch:   # manual trigger
  schedule:
    - cron: '0 * * * *'  # runs every hour at minute 0 UTC

jobs:
  process_rap:
    runs-on: ubuntu-latest

    steps:
      # ---------------- Checkout repo ----------------
      - name: Checkout repository
        uses: actions/checkout@v3

      # ---------------- Set up Python ----------------
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      # ---------------- Install dependencies ----------------
      - name: Install system dependencies for pygrib
        run: |
          sudo apt-get update
          sudo apt-get install -y libeccodes-dev

      - name: Install Python packages
        run: pip install pygrib numpy xarray

      # ---------------- Run process_rap.py ----------------
      - name: Run RAP processing script
        run: python scripts/process_rap.py

      # ---------------- Commit updated JSON ----------------
      - name: Commit updated tornado_prob.json
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}  # Your PAT token stored in repo secrets
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          # Use PAT for authentication
          git remote set-url origin https://x-access-token:${PAT_TOKEN}@github.com/alenzi35/tornado-probability.git
          git add map/data/tornado_prob.json
          # Commit only if there are changes
          git diff-index --quiet HEAD || git commit -m "Update tornado probability JSON"
          git push origin main
